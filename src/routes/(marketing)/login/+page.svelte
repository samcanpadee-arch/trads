<script lang="ts">
  // Adjust if your routes differ
  const SIGNUP = '/signup';
  const SIGNIN = '/login';
</script>

<!-- Minimal, responsive, no sales -->
<div class="min-h-[60vh] flex items-center">
  <div class="w-full max-w-md mx-auto px-4">
    <div class="card bg-base-100 border shadow-sm">
      <div class="card-body">
        <h1 class="text-3xl md:text-4xl font-bold">Get started</h1>
        <p class="opacity-80 mt-1">Create a free account or sign in.</p>

        <div class="mt-5 flex flex-col sm:flex-row gap-3">
          <a class="btn btn-primary btn-lg" href={SIGNUP} aria-label="Sign up">Sign up</a>
          <a class="btn btn-ghost" href={SIGNIN} aria-label="Sign in">Sign in</a>
        </div>
      </div>
    </div>
  </div>
</div>
